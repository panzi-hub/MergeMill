{
  "schema_version": 1,
  "description": "Minimal valid transitions instance (golden) — one dispatcher transition.",
  "states": [
    { "id": "MergeMill", "label": "MergeMill", "kind": "active", "mermaid": "MergeMill" },
    { "id": "in_progress", "label": "in-progress", "kind": "active", "mermaid": "in_progress" }
  ],
  "label_vocabulary": ["MergeMill", "in-progress"],
  "transitions": [
    {
      "id": "dispatch-new",
      "from": "MergeMill",
      "to": "in_progress",
      "event": "dispatcher-step2-scan-new",
      "actor": "dispatcher",
      "guards": ["deps-resolved"],
      "actions": ["add-label:in-progress", "comment:dispatching-dev"],
      "mermaid": "MergeMill --> in_progress: Dispatcher Step 2 scan-new"
    }
  ],
  "diagram": { "header": "stateDiagram-v2" }
}
